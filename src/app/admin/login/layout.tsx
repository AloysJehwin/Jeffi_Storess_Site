export default function LoginLayout({
  children,
}: {
  children: React.ReactNode
}) {
  // Just return children without any wrapping - the parent layout will handle HTML structure
  return children
}
